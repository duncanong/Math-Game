from tkinter import *
from tkinter import font
import time
import random

master = Tk()


#Fonts
#helv36=

  
#Functions

def quit_game():
    master.destroy()

#Difficulties

def easy_set():
    global ps
    global dif
    ps=1
    dif.set("Difficulty: Easy")
    Choose_mult=Button(master, text="Easy Multiplication", command=mp, bg='orange').grid(row=4,column=1)
    Choose_divt=Button(master, text="Easy Division", command=dp, bg='red').grid(row=5,column=1)
    Choose_addt=Button(master, text="Easy Addition",command=ap, bg='green').grid(row=2,column=1)
    Choose_subt=Button(master, text="Easy Subtraction", command=sp, bg='yellow').grid(row=3,column=1)
    #quit_game()
def med_set():
    global ps
    global dif
    ps=2
    dif.set("Difficulty: Medium")
    Choose_mult=Button(master, text="Medium Multiplication", command=mp, bg='orange').grid(row=4,column=1)
    Choose_divt=Button(master, text="Medium Division", command=dp, bg='red').grid(row=5,column=1)
    Choose_addt=Button(master, text="Medium Addition", command=ap, bg='green').grid(row=2,column=1)
    Choose_subt=Button(master, text="Medium Subtraction", command=sp, bg='yellow').grid(row=3,column=1)
    #Ez.config(state=DISABLED)
    #Hd.config(state=DISABLED)
    #quit_game()
def hard_set():
    global ps
    global dif
    ps=3
    dif.set("Difficulty: Hard")
    Choose_mult=Button(master, text="Hard Multiplication",command=mp, bg='orange').grid(row=4,column=1)
    Choose_divt=Button(master, text="Hard Division", command=dp, bg='red').grid(row=5,column=1)
    Choose_addt=Button(master, text="Hard Addition", command=ap, bg='green').grid(row=2,column=1)
    Choose_subt=Button(master, text="Hard Subtraction",command=sp, bg='yellow').grid(row=3,column=1)
    #quit_game()

#Math Problem Types





def mp():
    global ans
    global inp
    global ps
    if ps==1:
        a=random.randint(1,10)
        b=random.randint(1,10)
        prob=StringVar()
        prob.set(str(a)+" × "+str(b)+" = ")
        ans=a*b
        Pset=Label(master, textvariable=prob,bg='green').grid(row=6, column=0, padx=5)
        inp=Entry(master, text="Enter Your Answer Here",bg='gray').grid(row=6,column=1)
        sub_ans=Button(master, text="Submit Answer", command=check_answer, bg='red').grid(row=6, column=2)
    #elif ps==2:
        
    #elif ps==3:
        
    return True
def dp():
    global ps
    #if ps==1:
        
    #elif ps==2:
        
    #elif ps==3:
    return True
def ap():
    global ps
    #if ps==1:
        
    #elif ps==2:
        
    #elif ps==3:
    return True
def sp():
    global ps
    #if ps==1:
        
    #elif ps==2:
        
    #elif ps==3:
    return True

def check_answer():
    global ans
    global inp
    if ans==inp:
        Cor=Label(master, text="You Are Correct!", bg='green').grid(row=7, column=0)
    else:
        Fal=Label(master, text="You Are Incorrect!", bg='red').grid(row=7, column=1)

#Graphics

#Difficulties
dif=StringVar()
dif.set("Select Your Difficulty")
dif_choosen=Label(master, textvariable=dif,bg='black',fg='white').grid(row=0,column=1)
#ez_desc=Label(master, text="
#med_desc=Label(master, text="
#hard_desc=(master, text="
Ez=Button(master, text="Easy", command=easy_set,bg='green',).grid(row=1,column=0)
Md=Button(master, text="Medium", command=med_set,bg='yellow').grid(row=1,column=1)
Hd=Button(master, text="Hard", command=hard_set,bg='red',).grid(row=1,column=2)


#Math Problems
pt=StringVar()
pt.set("Select Your Problem ps Topic")

    






master.config(bg='black')
mainloop()
